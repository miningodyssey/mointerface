export type user = {
    balance: number
    referals: number
    referer: string | number
    upgrades: string
    personalRecord: number
    tgUserData: string
    registrationDate: Date
    completedTaskIds: number[]
};
